# Copy to .env and fill in your key from https://console.anthropic.com/settings/keys
# .env is gitignored — never commit a real key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model without editing code.
# MODEL=claude-opus-5
